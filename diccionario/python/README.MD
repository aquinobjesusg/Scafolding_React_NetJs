## Windows

# Linux - Habilitar el Ambiente
env\Scripts\activate.bat

# Linux - Instalar los Requerimientos
python -m pip install -r requirements.txt

# Correo el Proyecto de Python
python app.py

# Correr el Proyecto de StremLit
streamlit run simularvistabil08.py


## LINUX
# Instalar el Ambiente en Linux de Python
 sudo apt install python3.12-venv -y

# Linux - Habilitar el Ambiente
python3 -m venv env && source env/bin/activate

# Linux - Instalar los Requerimientos
python3 -m pip install -r requirements.txt

# Correo el Proyecto
python3 app.py

# Correr el Proyecto de StremLit
streamlit run simularvistabil08.py










#
streamlit run simularvistabil07.py

pip install streamlit
pip install psycopg2-binary
pip install SpeechRecognition
pip install gTTS

# Opcional
pip install pyaudio
pip install playsound


pip install reflex
reflex init
reflex run



import sqlite3

# Crea o se conecta a una base de datos existente
conexion = sqlite3.connect('mi_base_de_datos.db')

# Cierra la conexión al finalizar
conexion.close()




pip install flask
python app.py

python -m pip install -r requirements.txt

python simularvistabil08.py
